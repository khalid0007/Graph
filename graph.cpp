#include<bits/stdc++.h>
using namespace std;


const int N = 1e5 + 1; //maximum vertices + 1
vector<int> g[N];

int32_t main(){

    // if directed graph & a ---> b
    g[a].push_back(b);
    // if undirected graph a ---- b
    g[a].push_back(b);
    g[b].push_back(a);
}